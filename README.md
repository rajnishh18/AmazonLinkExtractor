# AmazonLinkExtractor
This Java project uses Selenium WebDriver to open Amazon's homepage and extract all hyperlink elements. It demonstrates web automation using ChromeDriver with implicit waits. The script counts and prints all anchor (&lt;a>) tag texts from the page. Ideal for beginners learning Selenium link scraping techniques.
